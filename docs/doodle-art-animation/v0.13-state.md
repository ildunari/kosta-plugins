# v0.13 state and what is left (2026-09-17)

Branch `doodle-v0.13`, 27 commits ahead of `main`, pushed. Nothing is merged to `main` yet and no PR is open.
The agreed scope is in `ACCEPTANCE.md` (ledger items L1–L17); the checks are
`tests/doodle-art-animation/acceptance_check.py`.

## Done and verified

- **All 60 static acceptance checks pass.** Per-lane full checks passed when each lane landed: L1/L2 (story),
  L3/L13 (speed_check, `--sheet-range`), L8 (brushes: determinism and frame time), L9 (kits: gallery builds,
  every component resolves, no page errors).
- Brushes (`brush.stroke` with 9 media, `brush.wash`/`wash`, `brush.hatch`, `brush.field`), ported natively from
  p5.brush's ideas and credited; `story_brushes.js` demo.
- `KIT.life` (6 components), `KIT.settle` (14), six new `KIT.earth` components; gallery is 10 plates.
- `speed_check.mjs` (engine-value speed, `FAST` advisory / `SNAP` fail) plus the `story_snap.js` fixture;
  `render.mjs --sheet-range A-B --fps N [--crop]`.
- Long Release title plate 0.88 → 1.41 and end card 0.79 → 1.45 (motion_check median), no SNAP, text_check CLEAN.
- `references/animation-principles.md`, `references/intake.md`; agents `script-reviewer`, `sound-designer`,
  `audio-reviewer`; doodle-qa runs all five agents.
- SKILL.md rewritten per the ledger (general hero, plates as scenes, night paper, full primitive list, no
  Water Cycle reference, no feedback loop, scene script + summary, intake step 0, `speed_check`, range sheets);
  `motion.md` has "Pace is a choice"; DEVELOPING.md updated; version 0.13.0 in `plugin.json` and the marketplace.

## Left to do

1. **One full acceptance run on the merged branch** (per-lane runs were on each lane's own base):
   `python3 tests/doodle-art-animation/acceptance_check.py --full --node-modules <playwright>/node_modules`
   (Playwright for this machine: `/private/tmp/claude-501/.../scratchpad/doodle-test/node_modules`, or install fresh).
2. **The [eye] items in ACCEPTANCE.md**: frame sheets for the brushes and every new component, the end card's
  closing beat, and a read-through of the principles, intake and agent files.
3. **Independent review** against ACCEPTANCE.md and the ledger, by an agent that did not build any of it.
4. **Render and send Kosta**: Long Release, One Drop, the brush demo and the gallery, plus frame sheets.
5. **PR → merge → `claude plugin marketplace update kosta-plugins` and `claude plugin update
   doodle-art-animation@kosta-plugins`**, then update the memory note.

## Known gaps, decided or accepted

- `story_brushes.js` measures a motion median of ~1.16 against the house target of 1.5 (the older
  `story_gallery.js` specimen sits at 1.40). Its last polish pass (livelier idles) was committed but **the motion
  numbers were not re-measured** — measure before calling L8's motion acceptable, or accept specimen films as an
  exception and say so in `motion.md`.
- `life.flock` murmuration reads as an even cloud rather than a folding one. Accepted after one polish pass.
- `fields` hedgerows and the modern `skyline` glazing are the most mechanical drawings in `KIT.settle`.
- The engine's `bleed` transition costs ~364 ms/frame before any brushes; wash-heavy films feel it at bleed seams.
  Pre-existing, not addressed.
- `village` calls `house` internally, so its line weights are very slightly heavy at composed scale.
